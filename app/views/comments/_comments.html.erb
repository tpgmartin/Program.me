<div id="comments">
<% @comments.each do |comment| %>
  <div class="comment">
    <%= link_to comment.user.try(:full_name) || "A user" %> wrote <%= comment.comment %> at <%= comment.created_at %>
  </div>
<% end %>
</div>

<%#= will_paginate @comments %>
